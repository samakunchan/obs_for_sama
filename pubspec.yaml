name: obs_for_sama
description: "A new Flutter project to manage OBS."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 0.20.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Project build with flutter bloc
  app_with_flutter_bloc:
    path: packages/app_with_flutter_bloc

  # Project build with GetX
  app_with_get_x:
    path: packages/app_with_get_x

  # Firebase
  firebase_core: ^4.1.0
  firebase_crashlytics: ^5.0.1

  # For windows (spécial demand)
  bitsdojo_window: ^0.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  very_good_analysis: ^9.0.0

flutter:

  uses-material-design: true
#  fonts:
#    - family: UnicaOne
#      fonts:
#        - asset: fonts/unica_one/UnicaOne-Regular.ttf
#          weight: 600
#    - family: Roboto
#      fonts:
#        - asset: fonts/roboto/Roboto-VariableFont_wdth,wght.ttf
#        - asset: fonts/roboto/Roboto_Condensed-SemiBold.ttf
#          weight: 700
#  assets:
#    - assets/
